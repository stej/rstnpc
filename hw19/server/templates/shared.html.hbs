{{! 
    how to correctly work with partials and layout was pretty tough to find. Finally found here: https://rustrepo.com/repo/sunng87-handlebars-rust-rust-template-engine
    maybe this works too: https://github.com/sunng87/handlebars-rust/blob/master/examples/partials.rs; https://github.com/sunng87/handlebars-rust/tree/master/examples/partials
}}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <!-- <link rel="stylesheet" href="https://unpkg.com/mvp.css">  -->
    <link rel="stylesheet" href="https://chr15m.github.io/minimal-stylesheet/minimal.css">
    <style>
        footer { 
            padding-top: 1em;
            font-size: smaller
        }
        table td { padding-left: 0.6em; }
    </style>
    <script type="text/javascript">
        const stringToColour = (str) => {
                        let hash = 0;
            str.split('').forEach(char => {
                hash = char.charCodeAt(0) + ((hash << 5) - hash)
            })
            let colour = '#'
            for (let i = 0; i < 3; i++) {
                const value = (hash >> (i * 8)) & 0xff
                colour += value.toString(16).padStart(2, '0')
            }
            return colour
            }
    </script>
    <title>{{title}}</title>
</head>
<body>
    <div> <a href="/messages">Messages</a> </div>
    <div> <a href="/users">Users</a> </div>
    {{> body }}
    
<footer>
    Rendered: {{rendered}}
</footer>
</body>
</html>
